/** @type {import('tailwindcss').Config} */
module.exports = {
  darkMode: 'class',  // ← Esta linha ativa o dark mode por classe
  content: [
    "./src/**/*.{html,ts}",
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}
